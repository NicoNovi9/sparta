Hello,

Thank you for disabling cuda_memtest. I ran a test job on an H200 node
(job 1215492) and the GPU memory stayed free for the whole run: my
application no longer fails.

In the same ticket, I would like to ask for one more thing. To analyse the
performance of my application I need NVIDIA Nsight Compute (ncu), which is
part of the CUDA toolkit on the cluster. At the moment it stops with:

  ERR_NVGPUCTRPERM - The user does not have permission to access NVIDIA GPU
  Performance Counters on the target device

As far as I understand, access to these counters is restricted to
administrators by default in the NVIDIA driver. NVIDIA describes how to
allow it for normal users here:

  https://developer.nvidia.com/ERR_NVGPUCTRPERM

Would it be possible to enable it on the V100 and H200 nodes, or on some
of them? If it is not possible for everyone, access for my user only would
also be fine.

Thank you,
Nicola Noventa
